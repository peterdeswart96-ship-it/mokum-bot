// Mokum Magic 8 Ball — Bubble teksten
// Config-bron (#75): één bron voor widget.js én de React-build.
// Bewerk de teksten in public/configs/default.json (bubble.texts), NIET hier.
import cfg from "../../public/configs/default.json"

export default cfg.bubble.texts
